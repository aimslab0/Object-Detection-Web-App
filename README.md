# Object-Detection-Web-App
User uploads image → YOLO detects objects → app displays detected image + detection count.
A simple Streamlit web app that detects objects in uploaded images using a pretrained YOLOv8 model.
